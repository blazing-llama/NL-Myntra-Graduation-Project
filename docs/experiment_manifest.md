# Experiment Manifest

One row per experiment. Updated the day it happens — not reconstructed at the end.

| ID | Hypothesis | Dataset | Method | Expected | Actual | Decision | Timestamp |
|---|---|---|---|---|---|---|---|
| EXP-001 | v2 A.2 source viability gate | 1,500 Play Store reviews (500 each: Myntra, AJIO, Nykaa Fashion), cleaned to 248/185/180 usable | `pipeline/relevance_prefilter.py`, minimal is_relevant-only prompt, `llama3.1:latest` via local Ollama (substituted for `hermes3:8b`, see FAILURES.md) | 1–3% relevant rate per v2's pre-registered estimate (~5–15 relevant per app from 500) | 2/248 (0.8%) Myntra, 2/185 (1.1%) AJIO, 1/180 (0.6%) Nykaa — all below even the low end of the expected range | All three `<10` on the gate table → Play Store downgraded to background/corroboration for all apps; reallocate primary discovery to Reddit/YouTube/Quora. See `docs/decisions/product-and-source-choice.md` | 2026-08-19 |
| EXP-002 | Codebook v2 category coverage (docs/codebook.md) | 613-item cleaned Play Store corpus (all three apps), keyword-scanned per barrier category | Manual keyword scan (`size/fit`, `price/discount`, `occasion/wear`, `quality/material`, `stock/available`, `forgot/later`, `wishlist/saved`) against full corpus, hand-reviewed for true positives vs. keyword-collision near-misses | Each of the 7 barrier categories would yield at least one genuine, corpus-grounded positive example | Only 2 of 7 categories (`quality_trust`, `availability_decay`) have a real positive example. 5 of 7 (`fit_size`, `price_certainty`, `occasion_styling`, `timing_forgetting`, `bookmark_not_intent` — the last only a borderline case) have **zero** clean positive examples; all keyword hits for those categories were post-purchase-logistics near-misses | Play Store alone cannot support a valid E1 accuracy measure for 5 of 7 categories — the exact categories the research questions (H1–H6) depend on most. Confirms and sharpens EXP-001's finding at the category level, not just the aggregate relevance rate. Do not freeze `docs/codebook.md` or write `agents/coding_agent_prompt.md` until positive examples for these 5 categories exist (expected from the Reddit corpus) and a second gold-set tranche (~20–30 items) is drawn from it and combined with the current 90 Play Store candidates into one full gold set. This is direct evidence for why the project needed multi-source triangulation (v2 Part A.3) rather than a single-source pilot. **Update, 2026-08-24 — human blind-labelling of the 90-item stratified sample confirms this finding rather than resolving it:** 8/90 (8.9%) labelled relevant, category counts `price_certainty`=4, `quality_trust`=3, `availability_decay`=1, and `fit_size`/`occasion_styling`/`timing_forgetting`/`bookmark_not_intent` all still at **zero**. This was a stratified sample deliberately weighted toward relevant content (every pilot-flagged-relevant item included, per `evals/sample_gold_set.py`), so the true unstratified relevance rate on a random Play Store sample is likely lower still than 8.9%. Frozen as `evals/gold_set/gold_set_playstore_tranche.jsonl` — tranche 1 of the eventual full gold set, not the final frozen set | 2026-08-19; updated 2026-08-24 |
| EXP-003 | v2 Part D.4 — discovery-engine webhook must be live-tested, not just statically validated, before being treated as a working deliverable | `workflows/discovery-engine-webhook.json`, imported into a real local n8n 2.35.7 instance (self-hosted via npx, no Docker available) | CLI import + REST activation + live `curl POST` to the webhook, covering: real-relevant text, not-relevant text, missing text (400 path), and inspection of raw execution data / SQLite `webhook_entity` table when things silently failed | Workflow imports, activates, and the webhook responds correctly end-to-end on the first real n8n instance it touches | Found and fixed 4 real bugs before it worked: (1) missing workflow-root `id` blocked CLI import, (2) `tags` as plain strings blocked CLI import, (3) webhook node missing `webhookId` silently broke production webhook registration (a live in-place PATCH looked like it fixed it but corrupted the registered path — required full delete + clean re-import), (4) `$env.GROQ_API_KEY` access is blocked by n8n's default `N8N_BLOCK_ENV_ACCESS_IN_NODE=true` setting, meaning the workflow as designed would silently fail on any standard n8n Cloud/self-host instance without that explicitly disabled | All 4 fixes applied to `workflows/discovery-engine-webhook.json` (bugs 1–3) or initially patched with `N8N_BLOCK_ENV_ACCESS_IN_NODE=false` (bug 4) to confirm the diagnosis. Confirmed working end-to-end: real classification, not-relevant classification, and the missing-text error path all returned correct responses. This validates the "never treat a workflow as working until live-tested" rule was the right call — every one of these 4 bugs was invisible to static validation. **Bug 4's fix superseded by EXP-004 below** — disabling the env-access security setting was rejected as routing around the design choice rather than fixing it | 2026-08-22 |
| EXP-004 | Replace the env-var-expression fix for bug 4 (EXP-003) with a proper n8n Credential, without disabling any security setting | Same local n8n instance and workflow as EXP-003 | Created a real `httpHeaderAuth` credential via the n8n REST API (`Authorization: Bearer <key>`), updated both Groq HTTP Request nodes in `workflows/discovery-engine-webhook.json` to `authentication: genericCredentialType` / `genericAuthType: httpHeaderAuth` referencing the credential (placeholder id in the committed file, since credentials are encrypted per-instance and can't be shared in JSON), re-imported with the real credential id substituted for local testing only, restarted with default `N8N_BLOCK_ENV_ACCESS_IN_NODE` (i.e. blocking restored, no override) | Pipeline works identically to the env-var version, with the actual security boundary intact | Confirmed: real classification (`is_relevant: true, primary_barrier: fit_size`), not-relevant classification, and the missing-text 400 path all returned correct responses with zero security settings weakened | This is the fix that ships in `workflows/discovery-engine-webhook.json`. Whoever hosts the workflow for real must create a "Header Auth" credential named "Groq API Key (Header Auth)" in their own n8n instance and map both Groq nodes to it — n8n's import UI prompts for this automatically when a referenced credential id doesn't exist locally. Documented in `docs/FAILURES.md`, 2026-08-22 entry | 2026-08-22 |
| EXP-005 | Final gold-set freeze (E1 circularity guard, `docs/hypotheses.md` gate) — merge tranche 1 (Play Store, 90 items) + tranche 2 (v2-classified corpus incl. App Store, 47 items) into one frozen evaluation set before `agents/coding_agent_prompt.md` is written | Tranche 2 candidates: 35 v2-relevant items across 5 sources (myntra, ajio, appstore_myntra, appstore_ajio, appstore_nykaa) + 12 keyword-flagged v2-not-relevant items (save/wishlist/bookmark/size/occasion language) as a recall check against classifier false negatives | Blind double-labeling by 2 independent labelers on all 47 tranche-2 items, then project-owner tie-break on disagreements. `evals/merge_gold_set.py` merged the resolved tranche 2 with the already-frozen tranche 1 into `evals/gold_set/gold_set_final_frozen.jsonl` | A workable frozen n≈130-140 gold set covering both Play Store and App Store, with at least one clean positive example for every category the corpus can support | **Inter-labeler agreement: 89.4% raw agreement on relevant/not-relevant (42/47 items), 100% category agreement on every item both labelers independently flagged relevant (18/18).** 5 disagreements (items 4, 7, 29, 36, 41) resolved by the project owner as tie-break. Final merged set: **n=137** (90 tranche 1 + 47 tranche 2). 29 relevant / 107 not-relevant / 1 unresolved-null (tranche 1's pre-existing item 87, still blank, carried forward not fixed). Category breakdown across the full frozen set: price_certainty=11, quality_trust=7, fit_size=6, availability_decay=5, **occasion_styling=0, timing_forgetting=0, bookmark_not_intent=0, other=0** | **`evals/gold_set/gold_set_final_frozen.jsonl` is now the frozen gold set — `gold_set_playstore_tranche.jsonl` (tranche 1) was a checkpoint, not the final artifact.** The recall-check supplement found zero new relevant items among the 12 keyword-flagged not-relevant candidates — no evidence the v2 classifier is under-flagging on these specific keyword triggers, though this is a small, targeted check, not a general recall audit. The three zero-count categories are **not an open gap**: `docs/research-findings.md` (survey n=32 + 6 interviews, logged 2026-08-25) is the sole evidence source for `occasion_styling`, `timing_forgetting`, and `bookmark_not_intent` — all three are interview-confirmed real (see that file's confirm/contradict matrix) but structurally invisible to review-corpus methodology; no further corpus search is expected to surface them. This closes the Aug 24 blueprint gate (`docs/hypotheses.md`, `docs/SESSION_HANDOFF.md`) and clears `agents/coding_agent_prompt.md` to be written per project ground rules | 2026-08-25 |
| EXP-006 | E1 — Coding accuracy, the only accuracy measure per `wishlist-conversion-blueprint-v2.md` Part E | `evals/gold_set/gold_set_final_frozen.jsonl` (n=137, 1 unresolved-null item excluded — 136 scored) | `evals/e1_gold_set_eval.py` — the frozen `agents/coding_agent_prompt.md` prompt (never tuned against this set) run via `openai/gpt-oss-20b` on Groq, one retry escalating to `openai/gpt-oss-120b`, against every gold-set item; precision/recall/F1 computed per category plus overall `is_relevant` accuracy | An honestly-reported number, whatever it lands on — not a target to tune toward (E1 circularity guard) | **Clean run, 0 classification failures. Overall `is_relevant` accuracy: 0.875.** Per-category: `availability_decay` P=1.000/R=1.000/F1=1.000 (n=5); `not_relevant` P=0.941/R=0.897/F1=0.919 (n=107); `fit_size` P=1.000/R=0.667/F1=0.800 (n=6); `quality_trust` P=0.833/R=0.714/F1=0.769 (n=7); `price_certainty` P=0.667/R=0.545/F1=0.600 (n=11). `occasion_styling`/`timing_forgetting`/`bookmark_not_intent`/`other` have no gold-set coverage (0 support) — cannot report a score, not a 0. This is the 3rd E1 run on this gold set (diagnostic re-runs, not prompt tuning — see the `price_certainty` note below); `price_certainty` recall moved 0.4 → 0.5 → 0.545 across the three runs without fully stabilizing, consistent with its small support (n=11, so each run swings ~1/11) rather than a real trend. Diagnosis from the earlier re-runs (transcribed on record, not repeated here): most `price_certainty` misses reflect the gold label applying a looser "price mentioned negatively" standard than the codebook's own stricter "explicit hesitation" definition — the classifier is often *more* faithful to the frozen codebook than the human label, not less accurate | **This is the number that goes in the deck, per the E1 circularity guard: measured once more for a clean 0-failure run, not re-run again to chase a higher number.** `full raw results: evals/e1_results.json` | 2026-08-26 |
| EXP-007 | E3 — Cross-model robustness (agreement, NOT validation, per `wishlist-conversion-blueprint-v2.md` Part E — "κ tells you the models agree, it does not tell you either is correct") | Full frozen gold set, n=136 scored (same set as EXP-006, 1 unresolved-null excluded) | `evals/e3_cross_model_agreement.py` — the identical frozen Coding Agent prompt run against both **Model A: `openai/gpt-oss-20b` (Groq)** and **Model B: `llama3.2:3b` (Ollama, local)** — substituted for the blueprint's specified `hermes3:8b`, which has never successfully pulled in this environment (`docs/FAILURES.md`, 2026-08-19). Cohen's κ computed via a hand-rolled implementation (`evals/e3_cross_model_agreement.py`'s `cohens_kappa()`, unit-tested against known reference values in `evals/test_e3_cross_model_agreement.py` — no scikit-learn dependency added) | Some positive agreement expected if the task is well-specified; this run was never executed against real data before now (built and unit-tested only) | **First real run. Raw agreement (primary_barrier): 0.301. Cohen's κ (primary_barrier): 0.159. Cohen's κ (is_relevant): 0.222.** By conventional κ bands this is "slight" agreement on the full 9-way barrier classification and "fair" on the binary relevance call — both models are running the same frozen prompt on the same items, so the disagreement is a real, honestly-reported finding, not a bug: `llama3.2:3b` (a much smaller, weaker model than the blueprint's intended `hermes3:8b`) is likely a meaningfully less capable classifier on this task, not just a "different but equally good" second opinion. **This should be reported in the deck as a real limitation of the substitute-model choice, not smoothed into a robustness claim** — the low κ does not cast doubt on E1's accuracy number (EXP-006), which is measured against human gold labels, not against Model B | `full raw results: evals/e3_results.json`. If a stronger, closer-to-`hermes3:8b` local model becomes available before submission, re-running E3 once with that substitution (logged as a new EXP row, not overwriting this one) would give a more blueprint-faithful robustness read | 2026-08-26 |
